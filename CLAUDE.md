# CLAUDE.md — 叙世者宇宙(galaxia-portal)项目记忆

婚礼策划品牌"叙世者"的官网/作品集/客户档案馆三合一站点。宇宙即产品:每对新人是一颗星,场地是星云,合作伙伴是彗星。总纲见 `docs/叙世者宇宙·架构蓝图 v1.1.md`(如尚未入库,请先向卷索取放入 docs/)。与卷沟通一律用中文,代码注释用中文。

## 不可violated的铁律

1. **渲染层锁版。** `js/pointillism-factory.js` 的点彩生成逻辑是多轮调校的定稿,禁止改动其粒子画法、密度、分层与配色归一化。粒子永远是哑光硬点,禁止 radial-gradient、shadowBlur、单粒子模糊发光;柔和只能来自疏密。Bloom 只服务发光星球本体,粒子绝不进泛光。
2. **相机规矩。** 全部镜头运动走统一的阻尼追赶/tween,且**每一帧驱动相机位置的同时必须 `camera.lookAt(controls.target)`**(跳切的根源就是只动位置不动朝向);严禁在动画结束时对 camera/controls.target 做一次性硬赋值;巡航永远锚定星图中心,画面里必须始终有星。
3. **缩放必须重生成。** 静止画面不允许是插值放大的贴图:基准 2048px + mipmap,聚焦后同 seed 重生成 4096px 替换,拉远换回并 dispose 释放。过渡动画期间(<1s)允许临时缩放。
4. **入口永远是天体本身。** 点星开档案、点星云开场地、点彗星开伙伴;连线只在悬停时浮现,作氛围不作入口。
5. **颜色填写制 + 归一化。** 每个案子的主/辅色由录入表单提供,渲染前必须归一到锁版色板区间(S 55~75%、L 按层规范),任何输入色不得破坏整体气质。全站 UI 为暖黑 #0c0c10 + 米金 #f3e9d2 + 暖金 #d4a95a + 衬线标题,禁止蓝色科技风。
6. **公私分层。** 数据从第一天起分 public/private 两层,私密内容(五域、成篇、方案)绝不进入公开渲染与搜索索引。

## 当前状态(接手点)

- P0 完成:点彩渲染定稿、three.js 底盘、暖金气质、年轮布局、发光星球本体、爆裂转场骨架。
- P1 完成:巡航锚定;单击连贯进入(点击→阻尼追赶居中→0.6s 蓄势→自动爆裂→二级页);二级页为"中央星尘核 + 五个大小悬殊的五域小星系散落四周,整体同转"(`js/interior.js` v2)。
- 最近一次修复:main.js 三处补 `camera.lookAt(controls.target)`(追赶/tween/巡航),interior.js 整体重写为中央聚尘构图。
- 数据仍是旧的 `data/clients.json`(企业字段假数据),尚未换血。

## 下一步(按蓝图 P2 施工)

按蓝图第四节 schema 重建数据层:systems / stories / venues / partners 四集合、公私分层、反向引用构建脚本;星位由 system 首段故事年份决定,回访以光环表达;星域带与星云渲染(复用点彩工厂低密度大尺寸配方,无核更弥散)、彗星带与彗星(小发光体+短粒子尾迹,缓慢巡游可点击);悬停关联点亮;场地/伙伴档案覆层页;15 组假数据按新人感情故事重写(含 6~8 朵星云、4~6 颗彗星)。

## 工作方式

- 每完成一个可运行的改动就 git commit,信息用中文写清改了什么。
- 视觉改动先用 `test/pointillism-shot.mjs` 截图自检(2048/4096 切换、进入流程、零 console 错误),再交卷给卷人工验收;卷的审美判断是最终标准。
- 大的结构改动(实体模型、页面层级、数据 schema)先提方案与卷确认,不要直接动手;蓝图未定义的问题,宁可问,不要发挥。
- 假数据中的新人故事文字是品牌脸面,写作要克制、具体、有细节,不要营销腔。
